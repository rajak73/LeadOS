// Placeholder dashboard. Real KPIs (Screen 1) land in Sprint 8. This proves the shell +
// tokens render.
export default function DashboardPage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold text-text-primary">LeadOS</h1>
      <p className="mt-2 text-text-secondary">
        Platform spine online. Dashboard arrives in a later sprint.
      </p>
    </section>
  );
}
