// External-API mock scaffolding (M7 seed). Sprint 1 has no external integrations to mock;
// this file establishes the location/pattern so Meta/Stripe/OpenAI/SendGrid mocks added in
// later sprints have a home. Domain test harnesses (tenant-aware) extend from here in S3.

export const EXTERNAL_MOCKS_PLACEHOLDER = true;
